[pytest]
testpaths = tests
addopts = --strict-markers
markers =
    slow: marks tests as slow (deselect with '-m \"not slow\"')
    requires_models: marks tests that need downloaded models or large artifacts
    integration: marks end-to-end integration tests
