"""Minimal setup.py for backwards compatibility with build systems that require it.
All configuration is in pyproject.toml - this file just enables editable installs.
"""
from setuptools import setup

# All configuration is in pyproject.toml
setup()
